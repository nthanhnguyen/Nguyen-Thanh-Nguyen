<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update A User</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="topnav">
        <a href="/">Home</a>
        <a href="/create">Create Users</a>
    </div>
    <div class="container">
        <div class="form-add-new">
            <form action="/update-user" method="post">
                <fieldset>
                    <legend>Update a User</legend>

                    <div class="message success" style="display: none;">
                        <p>
                            <%= typeof message !=='undefined' && message ? message : '' %>
                        </p>
                        <a href="/">Go to Homepage</a>
                    </div>

                    <% if (typeof error !=='undefined' && error) { %>
                        <div class="message error" style="display: none;">
                            <p>
                                <%= error %>
                            </p>
                        </div>
                        <% } %>

                            <div class="input-group">
                                <label>Email: </label>
                                <input type="text" name="email" value="<%= userEdit.email %>" readonly />
                            </div>
                            <div class="input-group">
                                <label>Name: </label>
                                <input type="text" name="name" value="<%= userEdit.name %>" required />
                            </div>
                            <div class="input-group">
                                <label>Age: </label>
                                <input type="number" name="age" value="<%= userEdit.age %>" />
                            </div>
                            <div class="input-group">
                                <label>City: </label>
                                <input type="text" name="city" value="<%= userEdit.city %>" />
                            </div>

                            <div class="button-group">
                                <button type="submit">Save</button>
                            </div>
                </fieldset>
            </form>
        </div>
    </div>

    <!-- Include the message handler script -->
    <script src="/js/messageHandler.js"></script>

</html>